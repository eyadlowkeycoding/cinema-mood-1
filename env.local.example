# Get a free API key (v3 auth) at: https://www.themoviedb.org/settings/api
# The app works fully without this key using a built-in curated dataset —
# add it to unlock the full TMDB catalog (thousands of titles, posters, trailers, etc).
TMDB_API_KEY=
